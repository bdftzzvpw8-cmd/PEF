node_modules/
*.log

# Platform exports contain live player data — keep them out of the repo.
exports/
*.xlsx
!test/fixtures/*.xlsx

# Player rosters carry names, emails and dates of birth — never commit one.
*roster*.csv
